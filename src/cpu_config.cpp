#include "cpu_config.hpp"
// Configuration is header-only so inference code can use the setting without
// introducing a runtime dependency. This translation unit exists to keep the
// component explicit for downstream builds.
