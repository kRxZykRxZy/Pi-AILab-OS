// The complete inference implementation is generated into the build tree.
// This keeps the existing source intact while applying the tokenizer byte-map
// correction during every clean build.
#include <inference_v4.cpp>
